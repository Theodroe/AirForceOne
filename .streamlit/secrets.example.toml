# 우선순위 1: Streamlit SQL connection (AWS RDS 등)
[connections.weather]
dialect = "mysql"
host = "your-rds-endpoint.ap-northeast-2.rds.amazonaws.com"
port = 3306
database = "weather_db"
username = "admin"
password = "your-password"

# 우선순위 2: 로컬 fallback
[mysql]
host = "localhost"
port = 3306
database = "weather_db"
user = "root"
password = "acorn1234"
charset = "utf8mb4"

[paths]
data_dir = "data"

[api]
SHORT_TERM_FORECAST_API_KEY = ""
REPORT_DATA_API_SERVICE_KEY = ""
